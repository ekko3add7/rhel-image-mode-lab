# rhel-image-mode-lab

This repository is intended to store the files and directory structure required at build time for an image-based architecture.
It provides a lab environment for organizing and managing the artifacts used during the image build process.
